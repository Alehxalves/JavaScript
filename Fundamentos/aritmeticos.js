const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - a
const multiplicacao = c * b
const divisao = d / b
const modulo = b % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)